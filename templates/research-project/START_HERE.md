# START HERE — HARC Project Zero-Context Onboarding

> **Language:** Chinese canonical: `START_HERE.zh-CN.md`; this English file is the synchronized mirror.

This file is the entry point for any AI Agent taking over this research project from zero context.

## Mandatory first step

Before substantive work:

1. read `HARC_MANIFEST.yaml`;
2. read `HARC_CONTEXT_INTERFACE.yaml`;
3. read `BOOTSTRAP_PROMPT.zh-CN.md`;
4. read `SESSION_CONTEXT_BOOTSTRAP.zh-CN.md`;
5. read `AGENTS.zh-CN.md`;
6. read `docs/working-memory.zh-CN.md` (Index);
7. read `docs/working-memory/current-focus.zh-CN.md`;
8. read `docs/working-memory/task-plan.zh-CN.md`;
9. skip Work Log by default;
10. selectively retrieve current state from the three long-term layers using manifest/context-interface task routing;
11. use `ONBOARDING_REPORT_TEMPLATE.zh-CN.md` to output a HARC Onboarding Report and confirm `HARC REPOSITORY CONTEXT — ACTIVE`;
12. begin substantive work only after the report passes and Repository Context is active; update Current Focus / Task Plan after substantial work cycles and update Work Log at appropriate milestones.

## Onboarding Report must include

- protocol version / adopted commit;
- canonical language;
- current human content commitments;
- current Form state;
- current `BLOCKING` clarifications;
- task-relevant `NON-BLOCKING` clarifications;
- Working / Approved Framework state;
- current Artifact state;
- known synchronization defects;
- permitted propagation path for the current request.

## Clarification-first

For high-impact uncertainty about claims, concepts, terminology/translation, scope, inference, or structure, do not guess:

`ambiguity -> Task Plan / Clarification -> human resolution -> Promotion -> Decision Log -> appropriate Long-Term Memory -> downstream propagation`

## Normal decision propagation

`human decision -> Decision Log -> appropriate Core -> Argument Map -> Artifact -> bilingual parity`

## Bilingual governance

Unless the project explicitly decides otherwise, Chinese is canonical and English is the synchronized mirror.

---

## Copyable bootstrap prompt

> See `BOOTSTRAP_PROMPT.zh-CN.md` for the full copyable startup prompt.
