# Approved Framework Snapshots

This directory holds framework snapshots that have received explicit human approval. An empty directory does not imply approval.

Add a snapshot only after the AHICP Framework Approval step has been completed. Existing approved snapshots must not be silently rewritten.
