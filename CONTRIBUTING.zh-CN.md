# 为 HARC Protocol 贡献

> **本中文文件是规范性基准；英文 `CONTRIBUTING.md` 是同步镜像。**

HARC 旨在成为一套开放、可检查的持久人机研究协作协议。

## 欢迎哪些贡献？

包括但不限于：

- 对概念模型的批评；
- 提升协议清晰度；
- 保持相同语义角色的替代仓库布局；
- 适用于书籍、论文、报告和跨学科项目的示例；
- 机器可读 schema；
- validation / linting 工具；
- Agent onboarding 工具；
- 关于 semantic drift、handoff quality 或 human approval interface 的研究；
- 文档与翻译。

## 保留来源属性

请区分：

- 对协议核心设计的修改；
- 实现建议；
- 可选扩展；
- 具体示例；
- 关于协议是否有效的经验性证据。

不要把贡献者的提议写成已经被接受的创始人原则。

## 双语贡献规则

HARC 的实质性 Markdown 文档以中文为 canonical、英文为同步 mirror。

因此，影响实质含义的贡献应当：

1. 先修改或新增中文规范版本；
2. 在同一 PR / 工作轮次中同步英文版本；
3. 不得只更新一种语言而把另一种语言留在过时状态；
4. 如果贡献者只能提供一种语言，应在 issue/PR 中明确标记为“translation incomplete”，在同步完成前不应视为完整规范修改。

语言中立的代码、schema、BibTeX 或原始数据无需重复，但其人类可读说明应双语。

## 建议工作流

1. 创建 issue 描述问题或提案。
2. 对重大协议修改，说明改变了哪个 invariant 以及为什么。
3. 在可行时，把规范性修改与示例分开。
4. 使用聚焦的 branch 与 pull request。
5. 行为改变时，同时更新文档、模板和双语 mirror。
6. 执行双语 parity check。

## 兼容原则

如果一个实现保留以下功能性区分，则可以被视为与 HARC 兼容：

- 人类内容意图；
- 人类形式意图；
- AI 操作性表示；
- 批准状态；
- 证据；
- 历史决定；
- 派生表达；
- 跨 Agent 持久化。

精确文件名和平台可以演进。

## 许可说明

贡献许可条款将与仓库的明确开放许可决定一起最终确定。在提交重大贡献前，请阅读 `LICENSE-DECISION.zh-CN.md`。
